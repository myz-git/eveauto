# EVE Auto 项目开发规则

## 1. 代码风格规范

### 1.1 Python编码规范
- 遵循 PEP 8 编码规范
- 使用 UTF-8 编码
- 每行代码不超过 120 个字符
- 使用 4 个空格进行缩进，禁止使用 Tab

### 1.2 命名规范
- 函数名：使用小写字母和下划线 `snake_case`，如 `safe_find_icon()`
- 变量名：使用小写字母和下划线，如 `region_full_right`
- 常量名：使用大写字母和下划线，如 `DEBUG_DIR`
- 类名：使用大驼峰命名法 `PascalCase`，如 `IconCNN`

### 1.3 中文注释规范
- 所有函数必须包含中文 docstring 说明其功能
- 复杂逻辑处必须添加中文注释
- 状态转换和重要决策点必须有注释说明

## 2. 项目结构规范

### 2.1 文件组织
```
eveauto/
├── icon/               # 图标文件目录
├── model_cnn/          # CNN模型文件目录
├── traindata/          # 训练数据目录
├── debug_screenshots/  # 调试截图目录
├── utils.py           # 通用工具函数
├── jump1.py           # 跳跃导航脚本（方式1）
├── jump2.py           # 跳跃导航脚本（方式2）
├── task.py            # 任务执行脚本
├── talk.py            # 对话交互脚本
├── navigate.py        # 导航脚本
└── task.log           # 运行日志文件
```

### 2.2 模块职责
- `utils.py`：所有通用功能函数，包括图像识别、OCR、日志等
- `jump*.py`：不同的跳跃导航策略实现
- 每个脚本文件应该有明确的单一职责

## 3. 图像识别规范

### 3.1 图标命名
- 图标文件命名格式：`{功能}-{版本}.png`
- 例如：`jump1-1.png`, `warp1-1.png`
- 相同功能的不同版本用数字区分

### 3.2 屏幕区域定义
- 所有屏幕区域必须在 `screen_regions` 字典中定义
- 区域命名要具有描述性，如 `full_right_panel`
- 坐标格式：`(x, y, width, height)`

### 3.3 图像识别参数
- 默认阈值：`threshold=0.85`, `cnn_threshold=0.8`
- 特殊情况可调整，但必须注释说明原因
- 使用 `safe_find_icon()` 而非直接调用底层函数

## 4. 状态机设计规范

### 4.1 状态定义
- 使用字符串定义状态，如 `"set_destination"`, `"warp"`
- 状态名必须清晰表达当前阶段的任务
- 避免使用缩写或模糊的状态名

### 4.2 状态转换
- 每个状态转换必须记录日志
- 状态转换条件必须明确
- 异常情况必须有处理分支

### 4.3 状态机模板
```python
while True:
    if state == "state_name":
        # 状态处理逻辑
        if condition:
            log_message("INFO", "状态转换说明")
            state = "next_state"
    elif state == "another_state":
        # 另一个状态的处理
        pass
    time.sleep(1)  # 避免CPU占用过高
```

## 5. 错误处理规范

### 5.1 异常捕获
- 主函数必须有全局异常捕获
- 关键操作使用 try-except 包裹
- 异常必须记录到日志

### 5.2 重试机制
- 图像识别默认重试次数通过 `max_attempts` 参数控制
- 超过最大重试次数必须记录错误日志
- 关键操作失败应该有降级方案

### 5.3 返回值规范
- 成功返回 `True` 或 `0`
- 失败返回 `False` 或错误码
- 特殊状态可返回具体值（如 `rolljump()` 返回 0 表示到达目的地）

## 6. 日志规范

### 6.1 日志级别
- `INFO`：正常操作和状态转换
- `WARNING`：可恢复的异常情况
- `ERROR`：操作失败或异常
- `DEBUG`：调试信息（生产环境应关闭）

### 6.2 日志内容
- 必须包含操作说明
- 关键操作包含坐标、参数等信息
- 错误日志必须截图 `screenshot=True`

### 6.3 日志格式
```python
log_message("INFO", "操作说明", screenshot=False)
logging.info("补充说明")  # 可选的额外日志
```

## 7. 性能优化规范

### 7.1 延时设置
- 操作后适当延时，避免过快导致识别失败
- 常规操作延时 0.5-1 秒
- 跳跃等待根据实际情况设置（如 60 秒）

### 7.2 资源管理
- 避免在循环中重复加载模型
- 大图像处理后及时释放内存
- 合理设置截图区域，避免全屏截图

### 7.3 并发控制
- 避免多个操作同时进行
- 使用状态机确保操作顺序
- 关键操作加锁防止冲突

## 8. 安全规范

### 8.1 操作安全
- 所有点击操作前必须确认目标存在
- 避免盲目点击造成误操作
- 关键操作前可加入确认机制

### 8.2 数据安全
- 不在代码中硬编码敏感信息
- 日志中避免输出敏感数据
- 调试截图定期清理

## 9. 测试规范

### 9.1 单元测试
- 工具函数必须有对应的测试
- 图像识别函数需要准备测试图片
- 测试覆盖正常和异常情况

### 9.2 集成测试
- 每个脚本必须能独立运行
- 提供测试模式，支持单步调试
- 记录测试结果和问题

## 10. 版本管理规范

### 10.1 提交规范
- 提交信息使用中文，清晰说明修改内容
- 功能性修改和bug修复分开提交
- 大型改动先创建分支

### 10.2 版本号规范
- 主版本号.次版本号.修订号
- 如：1.0.0, 1.1.0, 1.1.1

## 11. 特殊约定

### 11.1 坐标系统
- 基准分辨率：1920x1080
- 其他分辨率自动调整（使用 `adjust_region()` 函数）

### 11.2 OCR识别
- 优先使用 `find_txt_ocr()` 进行文字识别
- 特殊需求使用 `find_txt_ocr2()`
- OCR失败时考虑图像预处理

### 11.3 函数返回值约定
- `rolljump()` 类函数：
  - 返回 `True`：找到目标，正常退出
  - 返回 `False`：达到最大尝试次数
  - 返回 `0`：特殊状态（如到达目的地）

## 12. 维护规范

### 12.1 代码审查
- 新功能必须经过代码审查
- 重点检查图像识别准确性
- 确保日志记录完整

### 12.2 文档维护
- 代码修改同步更新文档
- 新增图标需要说明用途
- 维护图标和模型的对应关系 